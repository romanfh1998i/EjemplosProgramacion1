#include <stdio.h>
#include<stdlib.h>
int main(){
    int numeroleido,contador;
    printf("Escribe un numero");
    scanf("%d",&numeroleido);
    contador=1;
    while(contador<=10){
        printf("%d x %d=%d\n",numeroleido,c
        ontador,numeroleido*contador);
        contador++;
    }
}